#include "extra.h"
void main()//游戏主函数
{
	player *head = initialize(5, 5);//初始化一个地图5*5，并设定好路径
	player *position=head;//用于储存玩家最后的位置
	int e,f,i = 1;//计数变量
	int memory;//用于存储生成的随机数
	int money=10;
	int hp=100;//定义两个主要参数，money hp
	while (1)
	{
		gotoxy(7, 1);
		printf("hp=%d", hp);
		gotoxy(7, 2);
		printf("money=%d", money);//打印初始状态
		memory = suiji(6);//生成0到6的随机数，作为将要前进的步数
		gotoxy(6, 6);
		printf("\rpress enter to let the player go");
		getchar();
		Sleep(1000);
		jump(position, memory);//使玩家移动REMEMBER下
		for (i = 1;i <memory;i++)//使临时变量position跳转remember个位置，移动到此时玩家的所在，方便下一次继续移动
			position = position->next;
		runfunction(&hp, &money);
		gotoxy(7, 1);
		printf("hp=%d ", hp);
		Sleep(300);
		gotoxy(7, 2);
		printf("money=%d ", money);//进行操作后实时输出状态
		Sleep(300);
		if (hp == 0 || money>=200 )
			break;
	}
	Sleep(500);
	system("CLS");
	if (hp==0||money==0)
	{
		printf("YOU LOSE");
	}
	else
	{
		printf("YOU WIN");
	}
}



















































































































































































































































































































































































































































































